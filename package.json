{
  "name": "resolution-finder",
  "version": "1.0.0",
  "description": "A tool to determine screen geometry in both Wayland and X11 environments",
  "main": "module.js",
  "bin": {
    "resolution-finder": "./bin.js"
  },
  "type": "module",
  "scripts": {
    "start": "bun run module.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["screen", "geometry", "resolution", "wayland", "x11", "bun"],
  "license": "MIT",
  "engines": {
    "bun": ">=0.5.0"
  }
}
